
import{init,$,status,empty}from'./employer-admin-common.js';import{invokeEmployer,esc}from'./employer-management.js';
const d=await init('ders');if(d){const roles=new Set(['der','supervisor']);const members=d.members.filter(m=>roles.has(m.roles?.code));$('#managementContent').innerHTML=`<div class="management-table-wrap"><table class="management-table"><thead><tr><th>Name</th><th>Role</th><th>Status</th><th>Phone</th><th>Action</th></tr></thead><tbody>${members.length?members.map(m=>`<tr><td><strong>${esc(m.profiles?.display_name||[m.profiles?.first_name,m.profiles?.last_name].filter(Boolean).join(' ')||'Authorized User')}</strong></td><td><select data-role="${m.id}">${d.roles.filter(r=>['der','supervisor'].includes(r.code)).map(r=>`<option value="${r.id}" ${r.id===m.role_id?'selected':''}>${esc(r.name)}</option>`).join('')}</select></td><td>${esc(m.status)}</td><td>${esc(m.profiles?.phone||'—')}</td><td><button class="org-action" data-save="${m.id}">Save Role</button></td></tr>`).join(''):empty(5,'No DERs or supervisors assigned.')}</tbody></table></div>`;document.querySelectorAll('[data-save]').forEach(b=>b.onclick=async()=>{try{await invokeEmployer({action:'save_member',employer_id:d.employer.id,member:{id:b.dataset.save,role_id:document.querySelector(`[data-role="${b.dataset.save}"]`).value,status:'active'}});status('DER / supervisor role updated.','success')}catch(e){status(e.message,'error')}})}
