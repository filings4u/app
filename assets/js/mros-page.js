import{api,esc,wireSearch,msg}from'./admin-providers.js';const d=await api({action:'mros'}),rows=d.records||[],table=document.querySelector('#providerTable');function draw(a){table.innerHTML=`<thead><tr><th>MRO</th><th>License</th><th>Email</th><th>Phone</th><th>Status</th><th></th></tr></thead><tbody>${a.map(x=>`<tr><td><input data-name="${x.id}" value="${esc(x.name)}"></td><td><input data-license="${x.id}" value="${esc(x.license_number||'')}"></td><td><input data-email="${x.id}" value="${esc(x.email||'')}"></td><td><input data-phone="${x.id}" value="${esc(x.phone||'')}"></td><td><select data-status="${x.id}"><option ${x.status==='active'?'selected':''}>active</option><option ${x.status==='inactive'?'selected':''}>inactive</option></select></td><td><button class="org-action" data-save="${x.id}">Save</button></td></tr>`).join('')||'<tr><td colspan="6">No MRO records.</td></tr>'}</tbody>`;table.querySelectorAll('[data-save]').forEach(b=>b.onclick=async()=>{let x=rows.find(y=>y.id===b.dataset.save);for(const k of['name','license','email','phone'])x[k==='license'?'license_number':k]=table.querySelector(`[data-${k}="${x.id}"]`).value;x.status=table.querySelector(`[data-status="${x.id}"]`).value;try{await api({action:'save_mro',record:x});msg('MRO saved.','success')}catch(e){msg(e.message,'error')}})}wireSearch(rows,draw);