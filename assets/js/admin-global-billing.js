import { portalReady } from './portal.js?v=20260916-auth-isolation-v4';
await portalReady;
import{globalData,globalSave,filterRows,esc,money}from'./admin-global.js';const rows=await globalData('billing'),body=document.querySelector('#rows'),search=document.querySelector('#search');function draw(a){body.innerHTML=a.map(x=>`<tr><td><strong>${esc(x.invoice_number)}</strong></td><td>${esc(x.employers?.legal_name||'—')}</td><td>—</td><td>${money(x.total)}</td><td>${money(x.amount_due)}</td><td>${money(x.amount_paid)}</td><td><select data-id="${x.id}">${['draft','open','paid','past_due','void','uncollectible','refunded'].map(s=>`<option ${x.status===s?'selected':''}>${s}</option>`).join('')}</select></td></tr>`).join('')||'<tr><td colspan="7">No records.</td></tr>';body.querySelectorAll('[data-id]').forEach(s=>s.onchange=async()=>await globalSave({action:'save_invoice_status',id:s.dataset.id,status:s.value}))}filterRows(search,rows,draw);