import{overview,$}from'./employer-saas.js';let d;const csv=(rows,name)=>{if(!rows.length)return;const keys=Object.keys(rows[0]).filter(k=>typeof rows[0][k]!=='object');const q=v=>`"${String(v??'').replaceAll('"','""')}"`;const text=[keys.join(','),...rows.map(r=>keys.map(k=>q(r[k])).join(','))].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type:'text/csv'}));a.download=name;a.click();URL.revokeObjectURL(a.href)};overview().then(x=>d=x);document.querySelectorAll('[data-report]').forEach(b=>b.onclick=()=>{if(!d)return;const k=b.dataset.report,map={employees:d.employees,testing:d.testing_orders,compliance:d.compliance_cases,programs:d.programs};csv(map[k]||[],`${k}-report.csv`)})