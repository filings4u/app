import{init,$,status,empty}from'./employer-admin-common.js';import{invokeEmployer,esc}from'./employer-management.js';
const d=await init('dot-agencies');if(d){const agencies=d.agency_catalog||[],assigned=new Map((d.regulatory_agencies||[]).map(x=>[x.agency_code,x]));
$('#managementContent').innerHTML=`<div class="saas-notice"><strong>Access rule:</strong> the purchased plan determines software entitlement; agency assignment determines which regulatory version of that software is available. An FMCSA-only Employer therefore receives FMCSA workflows, not FAA/FRA/FTA/PHMSA/USCG workflows. Non-DOT is managed separately.</div><div class="management-table-wrap"><table class="management-table"><thead><tr><th>Agency</th><th>Primary regulation</th><th>Assigned</th><th>Primary</th><th>Account / Identifier</th></tr></thead><tbody>${agencies.filter(a=>a.code!=='NON_DOT').map(a=>{let x=assigned.get(a.code);return `<tr><td><strong>${esc(a.code)}</strong><br><small>${esc(a.name)}</small></td><td>${esc(a.primary_regulation||'—')}</td><td><input type="checkbox" data-agency="${a.code}" ${x?'checked':''}></td><td><input type="radio" name="primaryAgency" data-primary="${a.code}" ${x?.is_primary?'checked':''}></td><td><input data-identifier="${a.code}" value="${esc(x?.account_identifier||'')}"></td></tr>`}).join('')}</tbody></table></div><div class="saas-actions"><button class="btn btn-primary" id="saveAgencies">Save DOT Agency Assignments</button></div>`;
$('#saveAgencies').onclick=async()=>{const items=agencies.filter(a=>a.code!=='NON_DOT').map(a=>({agency_code:a.code,enabled:document.querySelector(`[data-agency="${a.code}"]`)?.checked||false,is_primary:document.querySelector(`[data-primary="${a.code}"]`)?.checked||false,account_identifier:document.querySelector(`[data-identifier="${a.code}"]`)?.value||null}));try{await invokeEmployer({action:'save_regulatory_agencies',employer_id:d.employer.id,items});status('DOT agency assignments saved.','success')}catch(e){status(e.message,'error')}}}