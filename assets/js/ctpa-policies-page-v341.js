import{init,$,empty}from'./ctpa-admin-common.js';import{op}from'./ctpa-ops.js';const d=await init('policies');if(d){const x=await op({action:'get',ctpa_id:d.ctpa.id});$('#content').innerHTML=`<div class="saas-notice"><strong>Plan-controlled:</strong> policy authoring is available only when Policy Builder is effectively enabled. DOT policies must follow the applicable agency rules; Non-DOT policies remain employer-policy/state-law specific.</div><div class="management-table-wrap"><table class="management-table"><thead><tr><th>Employer</th><th>Program</th><th>Agency</th><th>Title</th><th>Version</th><th>Status</th></tr></thead><tbody>${x.policies?.length?x.policies.map(p=>`<tr><td>${p.employers?.legal_name||'—'}</td><td>${p.program_type}</td><td>${p.dot_agency||'—'}</td><td>${p.title}</td><td>${p.version}</td><td>${p.status}</td></tr>`).join(''):empty(6,'No C/TPA-authored policies yet.')}</tbody></table></div>`}
