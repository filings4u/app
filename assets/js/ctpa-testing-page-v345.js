import { portalReady } from './portal.js?v=20260916-auth-isolation-v4';
await portalReady;
import{init,$,empty}from'./ctpa-admin-common.js';import{esc}from'./ctpa-management.js';const d=await init('testing');if(d){$('#content').innerHTML=`<div class="saas-notice"><strong>Provider-controlled lifecycle:</strong> This account view shows testing orders in the selected C/TPA scope. Use Platform Operations → Testing Orders for manual status administration so lifecycle changes follow the current platform status model.</div><div class="management-table-wrap"><table class="management-table"><thead><tr><th>Employer</th><th>Order</th><th>Employee</th><th>Reason</th><th>Test</th><th>Program</th><th>Status</th><th>Created</th></tr></thead><tbody>${d.testing_orders.length?d.testing_orders.map(x=>`<tr><td>${esc(x.employers?.legal_name||'—')}</td><td><strong>${esc(x.order_number)}</strong></td><td>${esc([x.employees?.first_name,x.employees?.last_name].filter(Boolean).join(' ')||'—')}</td><td>${esc(x.reason||'—')}</td><td>${esc(x.test_type||'—')}</td><td>${esc(x.programs?.name||x.program_type||'—')}</td><td><span class="saas-pill">${esc(String(x.status||'—').replaceAll('_',' '))}</span></td><td>${esc(x.created_at?new Date(x.created_at).toLocaleString():'—')}</td></tr>`).join(''):empty(8,'No testing orders in this C/TPA scope.')}</tbody></table></div>`}