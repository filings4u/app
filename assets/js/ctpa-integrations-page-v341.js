import{init,$,status,empty}from'./ctpa-admin-common.js';import{invokeCtpa,esc}from'./ctpa-management.js';const d=await init('integrations');if(d){$('#content').innerHTML=`<div class="management-table-wrap"><table class="management-table"><thead><tr><th>Integration</th><th>Type</th><th>Provider</th><th>Last Sync</th><th>Status</th><th></th></tr></thead><tbody>${d.integrations.length?d.integrations.map(x=>`<tr><td><strong>${esc(x.name||x.integration_type)}</strong></td><td>${esc(x.integration_type)}</td><td>${esc(x.provider||'—')}</td><td>${esc(x.last_sync_at||'—')}</td><td><select data-status="${x.id}"><option ${x.status==='active'?'selected':''}>active</option><option ${x.status==='inactive'?'selected':''}>inactive</option><option ${x.status==='disabled'?'selected':''}>disabled</option></select></td><td><button class="org-action" data-id="${x.id}">Save</button></td></tr>`).join(''):empty(6,'No integrations configured.')}</tbody></table></div>`;document.querySelectorAll('[data-id]').forEach(b=>b.onclick=async()=>{try{await invokeCtpa({action:'save_integration_status',ctpa_id:d.ctpa.id,integration:{id:b.dataset.id,status:document.querySelector(`[data-status="${b.dataset.id}"]`).value}});status('Integration status updated.','success')}catch(e){status(e.message,'error')}})}}
