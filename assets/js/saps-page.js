import{api,esc,wireSearch,msg}from'./admin-providers.js';const d=await api({action:'saps'}),rows=d.records||[],table=document.querySelector('#providerTable');function draw(a){table.innerHTML=`<thead><tr><th>SAP Organization</th><th>Email</th><th>Phone</th><th>Timezone</th><th>Status</th><th></th></tr></thead><tbody>${a.map(x=>`<tr><td><input data-name="${x.id}" value="${esc(x.legal_name)}"></td><td><input data-email="${x.id}" value="${esc(x.email||'')}"></td><td><input data-phone="${x.id}" value="${esc(x.phone||'')}"></td><td><input data-timezone="${x.id}" value="${esc(x.timezone||'')}"></td><td><select data-status="${x.id}"><option ${x.status==='active'?'selected':''}>active</option><option ${x.status==='inactive'?'selected':''}>inactive</option></select></td><td><button class="org-action" data-save="${x.id}">Save</button></td></tr>`).join('')||'<tr><td colspan="6">No SAP organizations.</td></tr>'}</tbody>`;table.querySelectorAll('[data-save]').forEach(b=>b.onclick=async()=>{let x=rows.find(y=>y.id===b.dataset.save);x.legal_name=table.querySelector(`[data-name="${x.id}"]`).value;for(const k of['email','phone','timezone'])x[k]=table.querySelector(`[data-${k}="${x.id}"]`).value;x.status=table.querySelector(`[data-status="${x.id}"]`).value;try{await api({action:'save_sap',record:x});msg('SAP provider saved.','success')}catch(e){msg(e.message,'error')}})}wireSearch(rows,draw);