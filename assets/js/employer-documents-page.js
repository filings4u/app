
import{init,$,empty}from'./employer-admin-common.js';import{esc}from'./employer-management.js';
const d=await init('documents');if(d){$('#managementContent').innerHTML=`<div class="saas-notice">Document records are scoped to this employer. File upload/storage controls will use the protected document-storage workflow rather than exposing storage paths directly in Admin.</div><div class="management-table-wrap"><table class="management-table"><thead><tr><th>Document</th><th>Type</th><th>Uploaded</th><th>Expires</th><th>Retention</th><th>Legal Hold</th><th>Access</th></tr></thead><tbody>${d.documents.length?d.documents.map(x=>`<tr><td><strong>${esc(x.file_name)}</strong></td><td>${esc(x.document_type)}</td><td>${esc(x.uploaded_at||'—')}</td><td>${esc(x.expires_at||'—')}</td><td>${esc(x.retention_until||'—')}</td><td>${x.legal_hold?'Yes':'No'}</td><td>${esc(x.access_level||'—')}</td></tr>`).join(''):empty(7,'No documents.')}</tbody></table></div>`}
