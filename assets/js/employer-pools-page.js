import{init,$,empty,status,formData}from'./employer-admin-common.js';import{invokeEmployer,esc}from'./employer-management.js';
const d=await init('pools');if(d){const agencies=(d.agency_catalog||[]).filter(a=>a.code!=='NON_DOT');$('#managementContent').innerHTML=`<div class="management-actions"><button class="btn btn-orange btn-compact" id="createPool">+ Create Random Pool</button></div><div class="saas-notice"><strong>Regulatory control:</strong> Random testing rates are shown from stored pool/regulatory configuration. Admin should use the current applicable agency rate rather than manually improvising a regulatory minimum.</div><div class="management-table-wrap"><table class="management-table"><thead><tr><th>Pool</th><th>Type</th><th>Program</th><th>Agency</th><th>Drug Rate</th><th>Alcohol Rate</th><th>Schedule</th><th>Status</th></tr></thead><tbody>${d.pools.length?d.pools.map(x=>`<tr><td><strong>${esc(x.name)}</strong></td><td>${esc(x.pool_type)}</td><td>${esc(x.program_type)}</td><td>${esc(x.dot_agency||'—')}</td><td>${x.drug_testing_rate??'—'}</td><td>${x.alcohol_testing_rate??'—'}</td><td>${esc(x.selection_schedule||'—')}</td><td>${esc(x.status)}</td></tr>`).join(''):empty(8,'No random pools configured.')}</tbody></table></div><section id="poolEditor" class="admin-inline-editor" style="display:none"><h3>Create Employer Random Pool</h3><form id="poolForm"><div class="saas-form-grid"><label><span>Pool Name *</span><input name="name" required></label><label><span>Program Type *</span><select name="program_type" id="poolType"><option value="DOT">DOT</option><option value="NON_DOT">Non-DOT</option></select></label><label><span>DOT Agency</span><select name="dot_agency" id="poolAgency">${agencies.map(a=>`<option value="${a.code}">${esc(a.code)} — ${esc(a.name)}</option>`).join('')}</select></label><label><span>Pool Type</span><select name="pool_type"><option value="employer">Employer standalone</option><option value="consortium">Consortium</option></select></label><label><span>Drug Rate (%)</span><input name="drug_testing_rate" type="number" min="0" max="100" step="0.01"></label><label><span>Alcohol Rate (%)</span><input name="alcohol_testing_rate" type="number" min="0" max="100" step="0.01"></label><label><span>Selection Schedule</span><select name="selection_schedule"><option value="quarterly">Quarterly</option><option value="monthly">Monthly</option><option value="annual">Annual</option></select></label><label><span>Effective Date</span><input name="effective_date" type="date"></label></div><p class="management-help">Rates are stored with the pool for auditability. Verify the current agency minimum before saving a DOT pool.</p><div class="saas-actions"><button type="button" class="btn btn-outline btn-compact" id="cancelPool">Cancel</button><button class="btn btn-primary btn-compact">Create Pool</button></div></form></section>`;$('#createPool').onclick=()=>$('#poolEditor').style.display='block';$('#cancelPool').onclick=()=>$('#poolEditor').style.display='none';$('#poolType').onchange=()=>{$('#poolAgency').disabled=$('#poolType').value==='NON_DOT'};$('#poolForm').onsubmit=async e=>{e.preventDefault();const x=formData(e.currentTarget);if(x.program_type==='NON_DOT')x.dot_agency=null;try{await invokeEmployer({action:'save_random_pool',employer_id:d.employer.id,pool:x});status('Random pool created.','success');setTimeout(()=>location.reload(),250)}catch(err){status(err.message,'error')}}}
