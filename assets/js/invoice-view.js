import{supabase}from'./supabase.js';const q=new URLSearchParams(location.search),id=q.get('invoice')||q.get('id'),token=q.get('token'),el=document.querySelector('#invoice'),money=(n,c='USD')=>new Intl.NumberFormat('en-US',{style:'currency',currency:c||'USD'}).format(Number(n||0)),esc=(v='')=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));try{const{data,error}=await supabase.functions.invoke('workforce-invoice-public',{body:{action:'view',invoice_id:id,token}});if(error)throw error;if(data?.error)throw new Error(data.error);const x=data.invoice,c=x.customer||{};el.innerHTML=`<div class="invoice-brand-head"><div><img src="images/logo.png" alt="screenings4u Workforce Compliance" style="max-width:220px;height:auto"><div class="invoice-brand-workforce">Workforce Compliance</div></div><div class="invoice-number-block"><strong>Invoice ${esc(x.invoice_number)}</strong><span>${esc(x.status)}</span></div></div><div class="invoice-meta-grid"><div><span>Bill To</span><strong>${esc(c.name||x.recipient_name||'Customer')}</strong><small>${esc(x.recipient_email||c.email||'')}</small></div><div><span>Invoice Date</span><strong>${x.issued_at?new Date(x.issued_at).toLocaleDateString():'Draft'}</strong><small>${x.due_at?'Due '+new Date(x.due_at).toLocaleDateString():'Due on receipt'}</small></div><div><span>PO Number</span><strong>${esc(x.purchase_order||'—')}</strong></div><div><span>Service Period</span><strong>${esc(x.service_period_start||'—')} – ${esc(x.service_period_end||'—')}</strong></div></div><h2>${esc(x.invoice_title||'Workforce Compliance Services')}</h2><div class="management-table-wrap"><table class="management-table"><thead><tr><th>Description</th><th>Qty</th><th>Unit</th><th>Amount</th></tr></thead><tbody>${(x.items||[]).map(i=>`<tr><td>${esc(i.description)}</td><td>${esc(i.quantity)}</td><td>${money(i.unit_amount,x.currency)}</td><td>${money(i.amount,x.currency)}</td></tr>`).join('')}</tbody></table></div><div class="invoice-totals"><p><span>Subtotal</span><strong>${money(x.subtotal,x.currency)}</strong></p><p><span>Tax</span><strong>${money(x.tax,x.currency)}</strong></p><p><span>Credits / Discounts</span><strong>-${money(x.credits,x.currency)}</strong></p><p class="grand"><span>Total</span><strong>${money(x.total,x.currency)}</strong></p><p><span>Paid</span><strong>${money(x.amount_paid,x.currency)}</strong></p><p class="grand"><span>Amount Due</span><strong>${money(x.amount_due,x.currency)}</strong></p></div>${x.notes?`<section class="invoice-note"><strong>Notes</strong><p>${esc(x.notes)}</p></section>`:''}${x.terms_text?`<section class="invoice-note"><strong>Terms</strong><p>${esc(x.terms_text)}</p></section>`:''}<div class="saas-actions"><button class="btn btn-outline" id="printInvoice">Print / Save PDF</button>${Number(x.amount_due)>0&&!['paid','void'].includes(x.status)?`<a class="btn btn-orange" href="checkout.html?invoice=${encodeURIComponent(id)}&token=${encodeURIComponent(token)}">Pay Invoice</a>`:''}</div>`;document.querySelector('#printInvoice').onclick=()=>window.print()}catch(e){el.innerHTML=`<div class="saas-notice">${esc(e.message||'Unable to load invoice.')}</div>`}
