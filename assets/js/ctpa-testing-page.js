import{init,$,status,empty}from'./ctpa-admin-common.js';import{invokeCtpa,esc}from'./ctpa-management.js';const d=await init('testing');if(d){$('#content').innerHTML=`<div class="management-table-wrap"><table class="management-table"><thead><tr><th>Employer</th><th>Order</th><th>Employee</th><th>Reason</th><th>Test</th><th>Status</th><th>Advance</th></tr></thead><tbody>${d.testing_orders.length?d.testing_orders.map(x=>{let n={ordered:'scheduled',scheduled:'collected',collected:'completed'}[x.status];return `<tr><td>${esc(x.employers?.legal_name||'—')}</td><td><strong>${esc(x.order_number)}</strong></td><td>${esc([x.employees?.first_name,x.employees?.last_name].filter(Boolean).join(' ')||'—')}</td><td>${esc(x.reason)}</td><td>${esc(x.test_type)}</td><td>${esc(x.status)}</td><td>${n?`<button class="org-action" data-id="${x.id}" data-emp="${x.employer_id}" data-next="${n}">Mark ${n}</button>`:'—'}</td></tr>`}).join(''):empty(7,'No testing orders.')}</tbody></table></div>`;document.querySelectorAll('[data-next]').forEach(b=>b.onclick=async()=>{try{await invokeCtpa({action:'save_test_status',ctpa_id:d.ctpa.id,test:{id:b.dataset.id,employer_id:b.dataset.emp,status:b.dataset.next}});status('Testing status updated.','success');document.dispatchEvent(new CustomEvent('s4u:data-changed'))}catch(e){status(e.message,'error')}})}}