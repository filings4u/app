import{api,esc,wireSearch,msg}from'./admin-providers.js';const d=await api({action:'laboratories'}),rows=d.records||[],table=document.querySelector('#providerTable');function draw(a){table.innerHTML=`<thead><tr><th>Laboratory</th><th>External ID</th><th>Integration Key</th><th>Status</th><th></th></tr></thead><tbody>${a.map(x=>`<tr><td><input data-name="${x.id}" value="${esc(x.name)}"></td><td><input data-external="${x.id}" value="${esc(x.external_id||'')}"></td><td>${esc(x.integration_key||'—')}</td><td><select data-status="${x.id}"><option ${x.status==='active'?'selected':''}>active</option><option ${x.status==='inactive'?'selected':''}>inactive</option></select></td><td><button class="org-action" data-save="${x.id}">Save</button></td></tr>`).join('')||'<tr><td colspan="5">No laboratories.</td></tr>'}</tbody>`;table.querySelectorAll('[data-save]').forEach(b=>b.onclick=async()=>{let x=rows.find(y=>y.id===b.dataset.save);x.name=table.querySelector(`[data-name="${x.id}"]`).value;x.external_id=table.querySelector(`[data-external="${x.id}"]`).value;x.status=table.querySelector(`[data-status="${x.id}"]`).value;try{await api({action:'save_laboratory',record:x});msg('Laboratory saved.','success')}catch(e){msg(e.message,'error')}})}wireSearch(rows,draw);