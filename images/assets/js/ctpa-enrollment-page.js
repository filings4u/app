import{init,$,empty}from'./ctpa-admin-common.js';import{op}from'./ctpa-ops.js';const d=await init('enrollment');if(d){const x=await op({action:'get',ctpa_id:d.ctpa.id});$('#content').innerHTML=`<div class="saas-notice"><strong>Plan-controlled:</strong> C/TPAs can issue enrollment agreements, consortium agreements, and enrollment certificates using tracked document numbers.</div><div class="management-table-wrap"><table class="management-table"><thead><tr><th>Issued</th><th>Type</th><th>Tracking</th><th>Title</th><th>Status</th></tr></thead><tbody>${x.enrollment?.length?x.enrollment.map(p=>`<tr><td>${new Date(p.issued_at).toLocaleDateString()}</td><td>${p.document_type}</td><td>${p.tracking_number}</td><td>${p.title}</td><td>${p.status}</td></tr>`).join(''):empty(5,'No C/TPA enrollment documents issued.')}</tbody></table></div>`}
